{
  "name": "{{projectName}}",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "hono": "^4.7.11",
    "mongoose": "^8.15.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
